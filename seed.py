"""
Seed script — no dummy data.

All data comes from real WordPress sites.
Add sites via http://localhost:5173/settings → Connected Sites → Add site.
After adding a site, click Sync — posts are pulled and agents run automatically.
"""
print("No seed data — add real WordPress sites via Settings → Connected Sites → Add site.")
print("After adding a site, click Sync and agents will run automatically.")
